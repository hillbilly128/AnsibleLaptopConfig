---
- name: Setup Laptop
  hosts: localhost
  become: true
  debugger: never
  vars:
    var_power_profile: intel-sst

  vars_prompt:
    - name: bw_user
      prompt: "Enter your Bitwarden user name"
      private: false

    - name: bw_pass
      prompt: "Enter your Bitwarden password"
      private: true

  tasks:
    - name: Main Play loop with Bitwarden
      block:
        - name: Get Bitwarden Login status
          ansible.builtin.command:
            argv:
              - bw
              - status
          register: bw_status_raw
          become: false
          changed_when: bw_status_raw.rc == 0

        - name: Expand Bitwarden status result
          ansible.builtin.set_fact:
            bw_status: "{{ bw_status_raw.stdout | ansible.builtin.from_json }}"
          become: false

        - name: Log out Bitwarden
          ansible.builtin.command:
            argv:
              - bw
              - logout
          when: bw_status.status != "unauthenticated"
          become: false
          register: bw_status_raw
          changed_when: bw_status_raw.rc == 0

        - name: Login to Bitwarden
          ansible.builtin.command:
            argv:
              - bw
              - login
              - --raw
              - "{{ bw_user | ansible.builtin.quote }}"
              - "{{ bw_pass }}"
          register: bwsessionid
          become: false
          changed_when: bwsessionid.rc == 0


        - name: Set Hostname
          ansible.builtin.hostname:
            name: merlinsbeard.home.lab
            use: systemd

        - name: Get current Tuned power profile
          ansible.builtin.command: tuned-adm active
          register: current_tuned_raw
          changed_when: false
          failed_when: false

        - name: Extract clean profile name
          ansible.builtin.set_fact:
            current_tuned_profile: "{{ current_tuned_raw.stdout | regex_replace('^Current active profile: (.*)$', '\\1') | trim | default('none') }}"

        - name: Set Power scheme
          ansible.builtin.command: tuned-adm profile {{ var_power_profile }}
          when: current_tuned_profile != var_power_profile
          register: set_power_scheme
          changed_when: set_power_scheme.rc = 0

        - name: Disable Sleep and hibernate
          ansible.builtin.systemd:
            name: "{{ item }}"
            masked: true
          loop:
            - sleep.target
            - suspend.target
            - hibernate.target
            - hybrid-sleep.target

        - name: Disable SELinux
          ansible.posix.selinux:
            state: disabled
            update_kernel_param: true

        - name: Update laptop
          ansible.builtin.dnf:
            name: "*"
            state: latest

        - name: Install user tools
          ansible.builtin.dnf:
            name: "{{ item }}"
            state: present
          loop:
            - firefox
            - syncthing
            - syncthing-tools
            - gvim
            - ansible
            - libxcrypt-compat

        - name: Add Flathub
          community.general.flatpak_remote:
            name: flathub
            state: present
            flatpakrepo_url: https://dl.flathub.org/repo/flathub.flatpakrepo

        - name: Install Flatpaks
          community.general.flatpak:
            name:
              - com.bitwarden.desktop
              - md.obsidian.Obsidian

        - name: Setup build environment
          block:
            - name: Add Codium repo
              ansible.builtin.yum_repository:
                name: gitlab.com_paulcarroty_vscodium_repo
                description: "vscodium repo"
                baseurl: https://paulcarroty.gitlab.io/vscodium-deb-rpm-repo/rpms/
                enabled: true
                gpgcheck: true
                gpgkey: https://gitlab.com/paulcarroty/vscodium-deb-rpm-repo/raw/master/pub.gpg
                metadata_expire: 1440
                repo_gpgcheck: true

            - name: Install VSCodium and OpenTofu
              ansible.builtin.dnf:
                name: "{{ item }}"
                state: present
              loop:
                - codium
                - opentofu
                - python3-pip

            - name: Install Ansible and Ansible Development tools
              ansible.builtin.pip:
                name: "{{ item }}"
                state: present
              loop:
                - ansible-dev-tools
                - vault-bitwarden-client

            - name: Install Bitwarden CLI Client
              ansible.builtin.unarchive:
                src: https://bitwarden.com/download/?app=cli&platform=linux
                dest: /usr/local/bin
                remote_src: true
                mode: "u=rwx,g=rx,o=rx"

            - name: Install VSCodium Extensions
              ansible.builtin.command: "codium --install-extension {{ item }}"
              become: false
              loop:
                - redhat.ansible
                - OpenTofu.vscode-opentofu
                - ms-python.python
                - GitHub.vscode-pull-request-github
        #        - GitHub.remotehub    - not in codium yet

            - name: Set Git name and email
              ansible.builtin.command:
                cmd: "{{ item }}"
              become: false
              loop:
                - git config set --global user.name 'Craig Hillbeck'
                - git config set --global user.email 'craig.hillbeck@gmail.com'

        - name: Setup Tailscale
          block:
            - name: Add Tailscale repo
              ansible.builtin.yum_repository:
                name: Tailscale-Stable
                description: Tailscale Stable
                baseurl: https://pkgs.tailscale.com/stable/fedora/$basearch
                repo_gpgcheck: true
                gpgkey: https://pkgs.tailscale.com/stable/fedora/repo.gpg
                gpgcheck: true
                enabled: true
                state: present

            - name: Install Tailscale
              ansible.builtin.dnf:
                name:
                  - tailscale
                  - headscale
                state: present

            - name: Enable Tailscale Service
              ansible.builtin.systemd_service:
                name: tailscaled
                state: started
                enabled: true

            - name: Check Tailscale status
              ansible.builtin.command: tailscale ip
              register: tailscale_status
              failed_when: false
              changed_when: false

            - name: Start Tailscale
              ansible.builtin.command:
                argv:
                  - tailscale
                  - up
                  - --ssh
                  - --accept-dns
                  - --accept-routes
                  - >-
                      --auth-key={{ lookup(
                        'community.general.bitwarden',
                        'Tailscale-auth-key',
                        field='password',
                        bw_session=bwsessionid.stdout
                      ) | first }}
              changed_when: tailscale_status.rc != 0

        - name: Install Webex
          ansible.builtin.dnf5:
            name: 'https://binaries.webex.com/WebexDesktop-CentOS-Official-Package/Webex.rpm'
            state: present
            disable_gpg_check: true

        - name: Install RPMFusion repo
          ansible.builtin.dnf5:
            name: 'https://mirrors.rpmfusion.org/free/fedora/rpmfusion-free-release-44.noarch.rpm'
            state: present
            disable_gpg_check: true

        - name: Install libavcodec-freeworld
          ansible.builtin.dnf5:
            name: libavcodec-freeworld
            state: present

      always:
        - name: Get Bitwarden Login status
          ansible.builtin.command:
            argv:
              - bw
              - status
          register: bw_status_raw
          become: false
          changed_when: bw_status_raw.rc == 0

        - name: Expand Bitwarden status result
          ansible.builtin.set_fact:
            bw_status: "{{ bw_status_raw.stdout | ansible.builtin.from_json }}"
          become: false

        - name: Log out off Bitwarden
          ansible.builtin.command:
            argv:
              - bw
              - logout
          when: bw_status.status != "unauthenticated"
          become: false
          register: bw_status_raw
          changed_when: bw_status_raw.rc == 0
